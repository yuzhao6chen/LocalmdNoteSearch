---
title: 本地索引缓存设计
tags:
  - 缓存
  - JSONL
  - 序列化
  - 索引
---
# 本地索引缓存设计

## 为什么需要缓存

如果每次搜索都重新扫描所有文件，用户体验会变慢。缓存可以把解析后的文档保存下来，
下次搜索时直接加载。

## JSON Lines

本项目使用 JSON Lines 保存缓存。第一行保存版本号和根目录，后续每一行保存一个文档。

## 缓存校验

加载缓存时会检查版本号和记录类型。如果缓存格式不正确，系统会提示用户重新构建索引。
