---
title: Rust 所有权与借用复习
tags: [rust, 所有权, 借用, 内存安全]
---
# Rust 所有权与借用复习

## 所有权

Rust 的所有权机制用于在没有垃圾回收器的情况下管理内存。每个值都有唯一的所有者，
当所有者离开作用域时，值会被自动释放。

## 借用

借用允许函数临时访问数据而不取得所有权。不可变借用可以同时存在多个，可变借用在同一
时间只能存在一个。

## 适合搜索的关键词

所有权 借用 内存安全 生命周期
