---
title: 本地知识库搜索排序策略
tags: [搜索, 排序, 倒排索引, 相关度]
---
# 本地知识库搜索排序策略

## 倒排索引

倒排索引用关键词映射到包含该关键词的文档。搜索时不需要遍历所有正文，而是可以直接
找到候选文档。

## 相关度评分

相关度评分不能只看字符串是否出现。标题命中、标签命中、小标题命中和正文命中的权重
应该不同。

## 多关键词

如果一个文档同时命中多个关键词，例如 搜索 和 排序，它应该比只命中单个关键词的文档
排名更靠前。
