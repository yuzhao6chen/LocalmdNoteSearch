Local search ranking notes

Tags: #search #ranking

Title matches should receive a higher score than body matches. Tag matches are
also important because tags usually describe the whole document.

When multiple query keywords appear in the same document, the result should get
a coverage bonus. Repeated body matches still matter, but they should not always
beat a strong title hit.
